<%= turbo_frame_tag folder do %>
  <%= form_with model: folder  do |f| %>
    <%= render 'shared/errors', object: folder %>

    <div class="mb-3 row">
      <div class="col-sm-2 col-form-label">
        <%= f.label :title, for: dom_id(folder, :title) %>
      </div>

      <div class="col-sm-10">
        <%= f.text_field :title, id: dom_id(folder, :title), class: 'form-control' %>
      </div>
    </div>

    <div class="mb-3 row">
      <div class="col-sm-2 col-form-label">
        <%= f.label :description, for: dom_id(folder, :description) %>
      </div>

      <div class="col-sm-10">
        <%= f.text_field :description, id: dom_id(folder, :description), class: 'form-control' %>
      </div>
    </div>

    <%= f.submit 'Submit', class: 'btn btn-primary' %>
  <% end %>
<% end %>
